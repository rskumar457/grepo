## Install the Backend

grepo needs a Python backend to parse your codebase.

**Requirements:** Python 3.10+

**Recommended:** Install via [uv](https://docs.astral.sh/uv/):
```bash
uv pip install grepo
```

**Alternatives:**
```bash
pipx install grepo
# or
pip install grepo
```
